/**
 * EXCEPTIONS AND ERROR HANDLING.
 *
 * <p>Custom exceptions such as AppointmentNotFoundException and
 * DentistUnavailableException, plus the global handler that turns them into a friendly
 * message on screen instead of a Java stack trace. This supports the "error-free, with
 * appropriate messages" requirement in the marking rubric.</p>
 */
package lk.icbt.cis6003.dentalclinic.exception;
