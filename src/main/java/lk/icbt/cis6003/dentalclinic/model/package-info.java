/**
 * DOMAIN MODEL - the objects the whole system is built around.
 *
 * <p>Contains the JPA entities (User, Patient, Appointment, Dentist, Treatment, Bill)
 * that map onto the MySQL tables. These classes appear directly in the UML class diagram
 * for Task A, so their fields, types and access modifiers are written out in full.</p>
 */
package lk.icbt.cis6003.dentalclinic.model;
