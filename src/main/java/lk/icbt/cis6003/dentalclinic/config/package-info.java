/**
 * CONFIGURATION - wiring and cross-cutting setup.
 *
 * <p>Spring Security rules (which pages need a login, which need the ADMIN role),
 * the BCrypt password encoder, session and cookie settings, and the Singleton
 * configuration holder for clinic-wide values such as the consultation fee.</p>
 */
package lk.icbt.cis6003.dentalclinic.config;
